openapi: 3.0.0
info:
  title: Raffle Service API
  version: 1.0.0

paths:
  /startup:
    get:
      summary: Get startup configuration
      responses:
        '200':
          description: Startup configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StartupResponse'

  /info/blockchain:
    get:
      summary: Get service information
      responses:
        '200':
          description: Service info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InfoBlockchainResponse'

  /raffle:
    get:
      summary: Get raffle summaries
      parameters:
        - in: query
          name: offset
          schema:
            type: number
            default: 0
        - in: query
          name: limit
          schema:
            type: number
            default: 10
        - in: query
          name: status
          schema:
            type: array
            items:
              type: string
              enum: [active, successful, failed]
          style: form
          explode: false
        - in: query
          name: tokenIds
          schema:
            type: array
            items:
              type: string
        - in: query
          name: tags
          schema:
            type: array
            items:
              type: string
        - in: query
          name: ids
          schema:
            type: array
            items:
              type: string
        - in: query
          name: text
          schema:
            type: string
        - in: query
          name: direction
          schema:
            type: string
            enum: [ASC, DESC]
        - in: query
          name: order
          schema:
            type: string
            enum: [height, deadline]
      responses:
        '200':
          description: Raffle list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RaffleSummaryResponse'
  /raffle/{raffleId}:
    get:
      summary: Get raffle details by ID
      parameters:
        - in: path
          name: raffleId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Raffle details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RaffleDetailResponse'
        '404':
          description: Raffle not found
  /raffle/{raffleId}/basket:
    get:
      summary: Get winner baskets of a raffle
      parameters:
        - schema:
            type: number
            default: 0
          in: query
          name: offset
        - schema:
            type: number
            default: 10
          in: query
          name: limit
        - schema:
            type: string
            enum:
              - non-empty
              - empty
          in: query
          name: share
        - schema:
            type: string
            enum:
              - non-empty
              - empty
          in: query
          name: gift
        - schema:
            type: number
          in: query
          name: index
        - schema:
            type: string
          in: path
          name: raffleId
          required: true
      responses:
        '200':
          description: Winner baskets list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WinnerBasketListResponse'

        '404':
          description: Raffle not found
  /activities:
    get:
      summary: Get activities
      parameters:
        - in: query
          name: raffleId
          schema:
            type: string

        - in: query
          name: offset
          schema:
            type: integer
            default: 0

        - in: query
          name: limit
          schema:
            type: integer
            default: 10

        - in: query
          name: type
          description: Filter activity types
          schema:
            type: string
            enum: [raffle_created, ticket_bought, voted, gift_added]
          style: form
          explode: false

        - in: query
          name: address
          description: Return only activities of the caller
          schema:
            type: string

      responses:
        '200':
          description: Raffle activities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RaffleActivityResponse'
  /raffles/{raffleId}/basket/{basketId}:
    get:
      summary: Get details of a winner basket
      parameters:
        - in: path
          name: raffleId
          required: true
          schema:
            type: string
        - in: path
          name: basketId
          required: true
          schema:
            type: number
      responses:
        '200':
          description: Winner basket details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WinnerBasketDetailResponse'
        '404':
          description: Basket not found
  /tokens:
    get:
      parameters:
        - schema:
            anyOf:
              - type: array
                items:
                  type: string
                maxItems: 100
              - type: string
          in: query
          name: tokenIds
          required: true
      responses:
        '200':
          description: tokens list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokensResponse'
        '404':
          description: tokens not found

components:
  schemas:
    #######################
    # Startup
    #######################
    StartupResponse:
      type: object
      properties:
        txFee:
          type: integer
          x-faker: 150000
        version:
          type: string
          x-faker: '1.0.0'
        verifiedTokens:
          type: array
          items:
            $ref: '#/components/schemas/VerifiedToken'
      required: [txFee, version, verifiedTokens]

    VerifiedToken:
      type: object
      properties:
        tokenId:
          type: string
          x-faker: b2dcea48caf0e73309138d659f6eb69d7ec8793dee989670c72dd4ffde7ebeb3
        name:
          type: string
          x-faker: rsBTC
      required: [tokenId, name]

    #######################
    # Blockchain Info
    #######################
    InfoBlockchainResponse:
      type: object
      properties:
        fee:
          type: object
          properties:
            tx:
              type: integer
              format: int64
            service:
              type: number
            implementer:
              type: number
            creation:
              type: integer
              format: int64
          required:
            - tx
            - service
            - implementer
            - creation
        height:
          type: number
      required:
        - fee
        - height

    #######################
    # Raffles
    #######################
    RaffleSummaryResponse:
      type: object
      properties:
        total:
          type: integer
          minimum: 1
          maximum: 1000
          x-faker: 23
        items:
          type: array
          items:
            $ref: '#/components/schemas/RaffleSummary'
      required: [total, items]

    RaffleSummary:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        picture:
          type: string
        token:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            decimals:
              type: number
              default: 0
            verified:
              type: boolean
          required: [id, name, decimals, verified]
        winnersCount:
          type: number
        giftCount:
          type: number
        tags:
          type: array
          items:
            type: string
        deadline:
          type: number
        amount:
          type: object
          properties:
            goal:
              type: integer
              format: int64
            raised:
              type: integer
              format: int64
          required: [goal, raised]
        ticketPrice:
          type: integer
          format: int64
        status:
          type: string
          enum: [active, successful, failed]
      required:
        - id
        - name
        - description
        - token
        - winnersCount
        - giftCount
        - deadline
        - amount
        - ticketPrice
        - status
    #######################
    # Raffle Detail
    #######################

    RaffleDetailResponse:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: string
        token:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
            decimals:
              type: number
              default: 0
            verified:
              type: boolean
          required: [id, name, decimals, verified]
        tags:
          type: array
          items:
            type: string
        pictures:
          type: array
          items:
            type: string
        giftCount:
          type: number
        winnersCount:
          type: number
        ticketPrice:
          type: integer
          format: int64
        deadline:
          type: number
        amount:
          type: object
          properties:
            goal:
              type: integer
              format: int64
            raised:
              type: integer
              format: int64
          required: [goal, raised]
        addresses:
          type: object
          properties:
            project:
              type: string
            implementer:
              type: string
            service:
              type: string
          required: [project, implementer, service]
        share:
          type: object
          properties:
            winner:
              type: number
            implementer:
              type: number
            service:
              type: number
          required: [winner, implementer, service]
        backers:
          type: number
        status:
          type: string
          enum: [active, successful, failed]
      required:
        - id
        - name
        - description
        - token
        - winnersCount
        - giftCount
        - deadline
        - amount
        - ticketPrice
        - status
        - pictures
        - addresses
        - share
        - backers
    RafflePrize:
      type: object
      properties:
        prizeId:
          type: string
        name:
          type: string
        image:
          type: string
          format: uri
        amount:
          type: integer
      required:
        - prizeId
        - name

    
    #######################
    # Winner Baskets
    #######################
    WinnerBasketListResponse:
      type: object
      properties:
        total:
          type: integer
        items:
          type: array
          items:
            $ref: '#/components/schemas/WinnerBasketSummary'
      required:
        - total
        - items

    #######################
    # Winner Baskets
    #######################
    WinnerBasketSummary:
      type: object
      properties:
        index:
          type: number
        share:
          type: number
        gifts:
          type: array
          items:
            $ref: '#/components/schemas/BasketGift'
      required:
        - index
        - share
        - gifts



    #######################
    # Basket Gift
    #######################
    BasketGift:
      type: object
      properties:
        tokenId:
          type: string
        amount:
          type: integer
          format: int64
      required:
        - tokenId
        - amount
    


    #######################
    # Tokens Response
    #######################
    TokensResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/TokenSummary'
      required:
        - items
    #######################
    # Token Summary
    #######################
    TokenSummary:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        decimals:
          type: number
          default: 0
        verified:
          type: boolean
      required: 
        - id
        - decimals
        - verified


    #######################
    # Winner Basket Detail
    #######################
    WinnerBasketDetailResponse:
      type: object
      properties:
        index:
          type: number
        type:
          type: string
          enum: [empty, shared]
        tokenId:
          type: string
        tokenName:
          type: string
        sharePercent:
          type: number
          format: float
        shareAmount:
          type: number
        gifts:
          type: array
          items:
            $ref: '#/components/schemas/BasketGift'
        transactions:
          type: array
          items:
            $ref: '#/components/schemas/BasketTransaction'
      required:
        - basketId
        - type
        - gifts
        - transactions
        - tokenId
        - tokenName

    BasketTransaction:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
          enum: [asset_unwrap, ticket_won, asset_added]
        amount:
          type: number
        wallet:
          type: string
        address:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - type
        - amount
        - wallet
        - address
        - createdAt

    #######################
    # Raffle Activity
    #######################
    RaffleActivityResponse:
      type: object
      properties:
        total:
          type: integer
          minimum: 1
          maximum: 1000
          x-faker: 23
        items:
          type: array
          items:
            $ref: '#/components/schemas/RaffleActivity'
      required:
        - total
        - items

    RaffleActivity:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
          enum:
            - raffle_created
            - ticket_bought
            - upvoted
            - downvoted
            - basket_won
            - gift_added
            - raising_money
        status:
          type: string
          enum:
            - successful
            - pending
            - canceled
        address:
          type: string
        amount:
          type: number
          nullable: true
        basketNumber:
          type: string
          nullable: true
        basketType:
          type: string
        raffleName:
          type: string
          nullable: true
        raffleId:
          type: string
        createdAt:
          type: integer
      required:
        - id
        - type
        - address
        - createdAt
