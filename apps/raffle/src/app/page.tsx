import { Typography } from '@ergo-raffle/ui-kit';
export default function Home() {
  return (
    <div>
      <Typography variant="title">Title</Typography>
      <Typography variant="subtitle">Subtitle</Typography>
      <Typography variant="h1">h1</Typography>
      <Typography variant="h2">h2</Typography>
      <Typography variant="h3">h3</Typography>
      <Typography variant="h4">h4</Typography>
      <Typography variant="h5">h5</Typography>
      <Typography variant="label">label</Typography>
      <Typography variant="input">input</Typography>
      <Typography variant="subtext1">subtext1</Typography>
      <Typography variant="subtext2">subtext2</Typography>
      <Typography variant="subtext3">subtext3</Typography>
      <Typography variant="subtext4">subtext4</Typography>
      <Typography variant="button1">button1</Typography>
      <Typography variant="button2">button2</Typography>
    </div>
  );
}
